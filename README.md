Отлично! Тогда добавляем **SSO (Single Sign-On)** для авторизации — это будет современно и красиво, плюс добавляет полезный опыт для senior backend-разработчика.

Я обновлю ТЗ и сразу включу SSO, чтобы мы видели **конечную цель**:

---

# **Обновленное Техническое Задание: Feed-проект с CQRS + SSO**

## **Цель проекта**

Создать высоконагруженный, масштабируемый feed-сервис (аналог Instagram/Twitter) с PHP (Laravel/Symfony), PostgreSQL, Redis, очередями (RabbitMQ/Kafka), разделением Command/Query (CQRS), полной системой автотестов и **SSO для авторизации**.

Проект должен позволять:

* Симулировать миллион пользователей и нагрузку
* Проверять работу всех компонентов: API, очередей, кэша, базы, Worker
* Современная авторизация через **SSO**, чтобы пользователи могли входить через сторонние сервисы (Google, GitHub, etc.)

---

## **Основные функции**

### 1️⃣ Пользователи

* Регистрация через **SSO (OAuth2 / OpenID Connect)**
* Возможность входа через сторонние провайдеры (Google, GitHub, Facebook…)
* Подписка на других пользователей (follows)
* Получение своего feed

### 2️⃣ Посты

* Создание поста (Command → Event → Worker → обновление feed)
* Получение feed пользователя (Query)
* Лайки постов (Command → Event → Worker → обновление feed / счетчика)

### 3️⃣ Feed

* Пагинация / infinite scroll
* Быстрая выдача через `user_feed` таблицу + Redis cache
* Eventual consistency

### 4️⃣ CQRS

* **Write Side:** команды на создание поста/лайка
* **Event:** фан-аут событий в очередь
* **Worker:** обновление read model (`user_feed`) и кэша
* **Read Side:** Query для feed

### 5️⃣ Очереди

* RabbitMQ / Kafka для асинхронного обновления feed
* Fan-out для подписчиков поста
* Retry и idempotency

### 6️⃣ Кэш

* Redis для:

    * Top posts
    * Feed последних N постов пользователя
    * Rate limiting (опционально)
* TTL и логика инвалидации

### 7️⃣ База данных

* PostgreSQL для write и read моделей
* Read model `user_feed` оптимизирована для быстрого чтения
* Репликация/шардирование (логическое)
* Тестовая база PostgreSQL для CI/CD и автотестов

---

## **Тестирование**

* **Unit:** модели, бизнес-логика команд и запросов
* **Integration:** API + DB + Cache + Queue + Worker + SSO flow
* **Functional / E2E:** пользователь через SSO публикует пост → Worker обновляет feed → подписчик получает feed
* **Load testing:** k6 / JMeter → миллионы запросов

---

## **Нагрузочное моделирование**

* Preload: 1 000 000 пользователей, 10 000 000 постов, 5 000 000 связей follow
* Симуляция:

    * 10–50k GET `/feed` запросов/сек
    * 1–5k POST `/post` запросов/сек
    * 5–10k лайков/сек

---

## **Tech Stack**

* **Backend:** PHP 8+, Laravel или Symfony
* **Database:** PostgreSQL (write + read model)
* **Cache:** Redis
* **Queue:** RabbitMQ или Kafka
* **Auth:** OAuth2 / OpenID Connect (Google, GitHub…)
* **Testing:** PHPUnit/PestPHP, Docker для тестовой БД
* **Load testing:** k6 или JMeter
* **Optional:** Docker Compose для локальной инфраструктуры

---

## **Ожидаемый результат**

1. Рабочее приложение feed с CQRS
2. Write/Read модели
3. Асинхронное обновление feed через Worker и очередь
4. Быстрая выдача feed через таблицу `user_feed` + Redis
5. Полное автотестирование всех компонентов
6. Возможность симуляции большого трафика
7. Современная авторизация через **SSO**
8. Документация по архитектуре и trade-offs

---

💡 **Следующий шаг:**

Теперь мы можем перейти к **первому маленькому этапу**, который будет:

* Настройка **проекта на Laravel/Symfony**
* Подключение PostgreSQL
* Настройка базовых моделей (`User`, `Post`, `Follow`)
* И базовых unit-тестов

Таким образом мы постепенно будем двигаться к CQRS, Redis, очередям, SSO и нагрузочному тестированию.

Если хочешь, я могу прямо сейчас составить **первый этап с конкретными задачами и тестами**, который ты будешь делать.

Хочешь, чтобы я это сделал?
